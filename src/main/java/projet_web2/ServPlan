package projet_web2;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/**
 * Servlet implementation class ServPlan
 */
@WebServlet("/ServPlan")
public class ServPlan extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    /**
     * @see HttpServlet#HttpServlet()
     */
    public ServPlan() {
        super();
        // TODO Auto-generated constructor stub
    }

	/**
	 * @see HttpServlet#doGet(HttpServletRequest request, HttpServletResponse response)
	 */
    protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

    HttpSession session = request.getSession();
/*		
		
		try {
			new Planning() // à faire
		} catch (Exception e) {
			session.setAttribute("err", 1);
		}

*/		
		request.getRequestDispatcher("planning.jsp").forward(request, response);
	}


	/**
	 * @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse response)
	 */
    protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		request.setCharacterEncoding("UTF-8");
/*	Utilisateur pseudo;
		Planning edt;
		pseudo = (Utilisateur)request.getAttribute("pseudo");
		HttpSession session = request.getSession();
		
		try {
      // à faire
			session.setAttribute("edt", edt);
		} catch (Exception e) {
			session.setAttribute("err", 1);
		}
		
		session.setAttribute("pseudo", pseudo);
*/
    request.getRequestDispatcher("planning.jsp").forward(request, response);
	}


}
